export function PoweredByMask({ className = "" }: { className?: string }) {
  return (
    <p className={`text-[11px] font-semibold uppercase tracking-normal text-gold/85 ${className}`}>
      Powered by MASK
    </p>
  );
}
